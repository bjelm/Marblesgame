<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/finalstartbg"
    android:ignoreGravity="@drawable/spelaknapp"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Start" >

    <ImageButton
        android:id="@+id/mute"
        android:layout_width="50dip"
        android:layout_height="50dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="72dp"
        android:layout_marginRight="30dp"
        android:background="@drawable/soundon" />

      <ImageButton
          android:id="@+id/inst"
          android:layout_width="150dip"
          android:layout_height="60dip"
          android:layout_above="@+id/mute"
          android:layout_alignLeft="@+id/spela"
          android:layout_marginBottom="147dp"
          android:background="@null"
          android:onClick="spelaInst"
          android:scaleType="fitXY"
          android:src="@drawable/instukknapp" />

      <ImageButton
          android:id="@+id/spela"
          android:layout_width="150dip"
          android:layout_height="60dip"
          android:layout_above="@+id/inst"
          android:layout_centerHorizontal="true"
          android:layout_marginBottom="27dp"
          android:background="@null"
          android:onClick="valjsvarighet"
          android:scaleType="fitXY"
          android:src="@drawable/playknapp" />

</RelativeLayout>